---
interface Props {
  title: string;
  link?: string;
  github?: string;
  colSpan?: number;
  rowSpan?: number;
}

const {title, colSpan, rowSpan} = Astro.props;

---

<div class="bg-[#1a3153] p-4 rounded-md transition-all hover:-translate-y-2 focus:-translate-y-2 " tabindex="0">
    <div class="flex justify-between items-center">
        <h2 class="text-2xl pb-2">{title}</h2>
        <div class="flex gap-2">
            <p>
                
            </p>
        </div>
    </div>
    <p>
        <slot />
    </p>
</div>